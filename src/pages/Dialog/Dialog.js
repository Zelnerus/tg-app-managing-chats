import styles from './Dialog.module.scss';

export const Dialog = () => {
    return(
        <div className={styles.dialog}>
            <span className={styles.header}>Дашборд диалога</span>

        </div>
    );
}